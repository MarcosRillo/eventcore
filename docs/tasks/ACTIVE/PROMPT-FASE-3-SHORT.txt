# FASE 3: PHPUnit Metadata Warnings
**Referencia:** docs/tasks/TASK-FASE-3-phpunit-warnings.md (leer primero)
**Branch:** fix/resolve-technical-debt
**Tiempo:** 90-120 minutos
**Filosofía:** CALIDAD > VELOCIDAD - UN ARCHIVO A LA VEZ

## OBJETIVO
Migrar 4-5 test files de `@test` annotations a `#[Test]` attributes.
Eliminar 14 deprecation warnings.
Resultado 3/5 archivos es éxito, 5/5 es ideal.

## ESTRATEGIA
1. Verificar estado inicial (CHECKPOINT 0: 26/26 tests, ~14 warnings)
2. Identificar archivos con `grep -rn "@test" tests/Feature/`
3. Para CADA archivo (uno por uno):
   a. Hacer backup (.backup)
   b. Agregar import: `use PHPUnit\Framework\Attributes\Test;`
   c. Reemplazar `/** @test */` por `#[Test]`
   d. Verificar SOLO ese archivo: `--filter=NombreTest`
   e. Si falla → restaurar backup, continuar con siguiente
   f. Si pasa → git add (NO commit aún)
4. Verificar suite completa (CHECKPOINT FINAL: 26/26, 0 warnings)
5. Commit todos los archivos migrados exitosamente

## ARCHIVOS A MIGRAR (típicos)
- tests/Feature/ApprovalTest.php (~6 tests)
- tests/Feature/CategoryTest.php (~5 tests)
- tests/Feature/EventTest.php (~8 tests)
- tests/Feature/LocationTest.php (~5 tests)

## PATRÓN DE MIGRACIÓN
ANTES:
```php
/**
 * @test
 */
public function test_can_approve_event()
```

DESPUÉS:
```php
use PHPUnit\Framework\Attributes\Test;  // ← Al inicio

#[Test]  // ← Reemplazar
public function test_can_approve_event()
```

## REGLAS CRÍTICAS
- Tests DEBEN ser 26/26 SIEMPRE (no negociable)
- Migrar UN archivo a la vez (no todos juntos)
- Verificar cada archivo ANTES de continuar con siguiente
- Si un archivo falla → revertir SOLO ese archivo
- Backup obligatorio antes de editar cada archivo
- Import debe ir después de namespace, antes de class

## CHECKPOINTS OBLIGATORIOS
**CHECKPOINT 0:** Estado inicial
- [ ] Tests: 26/26 passing
- [ ] Warnings: ~14 (anotar cantidad exacta)

**POR CADA ARCHIVO:**
- [ ] Backup creado (.backup)
- [ ] Import agregado
- [ ] Todas @test reemplazadas
- [ ] Tests de ese archivo pasan: `--filter=NombreTest`
- [ ] No warnings para ese archivo
- [ ] git add (si pasó)

**CHECKPOINT FINAL:**
- [ ] Suite completa: 26/26 passing
- [ ] Warnings: 0 (antes: ~14)
- [ ] Mismo assertions count
- [ ] No errores PHP

## ROLLBACK SI ARCHIVO FALLA
```bash
# Restaurar backup del archivo problemático
cp tests/Feature/ApprovalTest.php.backup tests/Feature/ApprovalTest.php

# Verificar que vuelve a pasar
docker exec plataforma-calendario-backend php artisan test --filter=ApprovalTest

# Continuar con siguiente archivo
```

## COMMIT (después de migrar todos los exitosos)
```bash
git add tests/Feature/
git commit -m "refactor(tests): migrate PHPUnit annotations to PHP 8 attributes

- Replace @test annotations with #[Test] attributes
- Resolves X PHPUnit 12 deprecation warnings
- All tests still passing (26/26)

Files migrated:
- tests/Feature/ApprovalTest.php
- tests/Feature/CategoryTest.php
- [listar archivos migrados exitosamente]

Verification:
✅ Tests: 26/26 passing
✅ Warnings: 0 (before: 14)
✅ No regressions

Resolves: TD-002 from TECHNICAL-DEBT-INVENTORY.md"
```

## RESULTADO ESPERADO
- Score: 9.94 → 10.0
- Warnings: 14 → 0
- Files migrated: 3-5
- Tests: 26/26
- 1 commit limpio

## ARCHIVO DE REFERENCIA
Leer docs/tasks/TASK-FASE-3-phpunit-warnings.md para:
- Patrón de migración completo
- Troubleshooting detallado
- Ejemplos de código
- Checklist exhaustivo por archivo

EJECUTAR ARCHIVO POR ARCHIVO - NO TODOS JUNTOS.