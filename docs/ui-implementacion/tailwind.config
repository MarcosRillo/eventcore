// tailwind.config.ts - PALETA HÍBRIDA PROFESIONAL v1.0.0
// Fecha: Noviembre 12, 2025
// WCAG 2.1 Level AA Compliant

import type { Config } from 'tailwindcss'

const config: Config = {
  content: [
    './src/pages/**/*.{js,ts,jsx,tsx,mdx}',
    './src/components/**/*.{js,ts,jsx,tsx,mdx}',
    './src/app/**/*.{js,ts,jsx,tsx,mdx}',
    './src/features/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // ==========================================
        // PRIMARY COLORS (Azul Institucional)
        // ==========================================
        // Uso: Botones principales, links, autoridad
        // Contrast: 7.8:1 (AAA) on white
        primary: {
          50: '#eff6ff',
          100: '#dbeafe',
          200: '#bfdbfe',
          300: '#93c5fd',
          400: '#60a5fa',
          500: '#2563eb',  // MAIN - Botones primary
          600: '#1d4ed8',  // Hover
          700: '#1e40af',  // Active/Pressed
          800: '#1e3a8a',
          900: '#1e3a70',
        },

        // ==========================================
        // SECONDARY COLORS (Verde Tucumán)
        // ==========================================
        // Uso: Identidad local, success, naturaleza
        // Contrast: 4.6:1 (AA) on white
        secondary: {
          50: '#f0fdf4',
          100: '#dcfce7',
          200: '#bbf7d0',
          300: '#86efac',
          400: '#4ade80',
          500: '#10b981',  // MAIN - Success, verde
          600: '#059669',  // Hover
          700: '#047857',  // Active
          800: '#065f46',
          900: '#064e3b',
        },

        // ==========================================
        // ACCENT COLORS (Naranja Energía)
        // ==========================================
        // Uso: CTAs urgentes, destacados
        // Contrast: 5.1:1 (AA) on white (usar 600, no 500)
        accent: {
          50: '#fff7ed',
          100: '#ffedd5',
          200: '#fed7aa',
          300: '#fdba74',
          400: '#fb923c',
          500: '#f97316',
          600: '#ea580c',  // MAIN - Usar este para texto
          700: '#c2410c',
          800: '#9a3412',
          900: '#7c2d12',
        },

        // ==========================================
        // NEUTRAL PALETTE (Grises Profesionales)
        // ==========================================
        // Uso: Texto, backgrounds, borders
        // Contrast: 18.2:1 (AAA) for 900 on white
        neutral: {
          50: '#f8fafc',   // Backgrounds cards
          100: '#f1f5f9',
          200: '#e2e8f0',  // Borders cards
          300: '#cbd5e1',  // Borders inputs
          400: '#94a3b8',  // Text disabled
          500: '#64748b',  // Text secondary light
          600: '#475569',  // Text secondary
          700: '#334155',
          800: '#1e293b',  // ⚠️ NO usar como background!
          900: '#0f172a',  // Text primary
        },

        // ==========================================
        // SEMANTIC COLORS (Estados de Sistema)
        // ==========================================
        semantic: {
          success: {
            DEFAULT: '#10b981',  // secondary-500
            light: '#dcfce7',    // secondary-100
            dark: '#065f46',     // secondary-800
          },
          warning: {
            DEFAULT: '#f59e0b',  // amber-500
            light: '#fef3c7',    // amber-100
            dark: '#92400e',     // amber-800
          },
          error: {
            DEFAULT: '#dc2626',  // red-600
            light: '#fee2e2',    // red-100
            dark: '#991b1b',     // red-800
          },
          info: {
            DEFAULT: '#2563eb',  // primary-500
            light: '#dbeafe',    // primary-100
            dark: '#1e40af',     // primary-700
          },
        },

        // ==========================================
        // ROLE COLORS (Identificación por Rol)
        // ==========================================
        role: {
          organizer: {
            DEFAULT: '#2563eb',  // primary-500
            light: '#dbeafe',
            dark: '#1e40af',
          },
          entityAdmin: {
            DEFAULT: '#7c3aed',  // violet-600
            light: '#ede9fe',
            dark: '#6d28d9',
          },
          public: {
            DEFAULT: '#10b981',  // secondary-500
            light: '#dcfce7',
            dark: '#047857',
          },
        },

        // ==========================================
        // CATEGORY COLORS (Categorías de Eventos)
        // ==========================================
        category: {
          gastronomia: {
            DEFAULT: '#ea580c',  // accent-600 (mejor contraste)
            light: '#ffedd5',
          },
          cultura: {
            DEFAULT: '#7c3aed',  // violet-600
            light: '#ede9fe',
          },
          naturaleza: {
            DEFAULT: '#10b981',  // secondary-500
            light: '#dcfce7',
          },
          deportes: {
            DEFAULT: '#2563eb',  // primary-500
            light: '#dbeafe',
          },
          musica: {
            DEFAULT: '#ec4899',  // pink-600
            light: '#fce7f3',
          },
          historia: {
            DEFAULT: '#b45309',  // amber-700
            light: '#fef3c7',
          },
        },
      },

      // ==========================================
      // TYPOGRAPHY
      // ==========================================
      fontSize: {
        xs: ['0.75rem', { lineHeight: '1rem' }],       // 12px
        sm: ['0.875rem', { lineHeight: '1.25rem' }],   // 14px
        base: ['1rem', { lineHeight: '1.5rem' }],      // 16px (DEFAULT)
        lg: ['1.125rem', { lineHeight: '1.75rem' }],   // 18px
        xl: ['1.25rem', { lineHeight: '1.75rem' }],    // 20px
        '2xl': ['1.5rem', { lineHeight: '2rem' }],     // 24px
        '3xl': ['1.875rem', { lineHeight: '2.25rem' }], // 30px
        '4xl': ['2.25rem', { lineHeight: '2.5rem' }],  // 36px
        '5xl': ['3rem', { lineHeight: '1' }],          // 48px
      },

      fontWeight: {
        normal: '400',
        medium: '500',
        semibold: '600',
        bold: '700',
      },

      // ==========================================
      // SPACING (Tailwind default 8px system)
      // ==========================================
      // Ya incluido por defecto en Tailwind
      // Documentado aquí para referencia

      // ==========================================
      // SHADOWS
      // ==========================================
      boxShadow: {
        sm: '0 1px 2px 0 rgb(0 0 0 / 0.05)',
        DEFAULT: '0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1)',
        md: '0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1)',
        lg: '0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1)',
        xl: '0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1)',
        '2xl': '0 25px 50px -12px rgb(0 0 0 / 0.25)',
        inner: 'inset 0 2px 4px 0 rgb(0 0 0 / 0.05)',
        none: 'none',
      },

      // ==========================================
      // BORDER RADIUS
      // ==========================================
      borderRadius: {
        none: '0',
        sm: '0.125rem',    // 2px - Inputs
        DEFAULT: '0.25rem', // 4px - Buttons, cards
        md: '0.375rem',    // 6px - Modals
        lg: '0.5rem',      // 8px - Sections
        xl: '0.75rem',     // 12px
        '2xl': '1rem',     // 16px
        '3xl': '1.5rem',   // 24px
        full: '9999px',    // Pills, avatars
      },

      // ==========================================
      // TRANSITIONS
      // ==========================================
      transitionDuration: {
        DEFAULT: '200ms',
        fast: '150ms',
        slow: '300ms',
      },

      // ==========================================
      // Z-INDEX SCALE
      // ==========================================
      zIndex: {
        dropdown: '1000',
        sticky: '1020',
        fixed: '1030',
        modalBackdrop: '1040',
        modal: '1050',
        popover: '1060',
        tooltip: '1070',
      },
    },
  },
  plugins: [],
}

export default config

// ==========================================
// MIGRATION NOTES
// ==========================================
// 
// REPLACE THESE OLD COLORS:
// - bg-slate-800 → bg-white (CRITICAL en modals)
// - text-gray-400 → text-neutral-600
// - text-gray-600 → text-neutral-900
// - bg-blue-600 → bg-primary-500
// - bg-green-600 → bg-secondary-500
// - border-gray-600 → border-neutral-300
// - border-gray-300 → border-neutral-200
//
// COMMON PATTERNS:
// - Primary button: bg-primary-500 hover:bg-primary-600
// - Secondary button: bg-neutral-50 hover:bg-neutral-100 border-neutral-300
// - Input: bg-neutral-50 border-neutral-300 focus:border-primary-500
// - Card: bg-white border-neutral-200 shadow-sm
// - Text primary: text-neutral-900
// - Text secondary: text-neutral-600
// - Success: bg-secondary-100 text-secondary-800 border-secondary-500
//
// ==========================================