/**
 * Locations Page
 * Server Component that renders the locations page container
 */

import { LocationsPageContainer } from '@/features/locations/components/smart/LocationsPageContainer'

export default function LocationsPage() {
  return <LocationsPageContainer />
}
