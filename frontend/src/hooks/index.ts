/**
 * Hooks Export Index
 */

export { useDebounce } from '@/shared/hooks/useDebounce';
export { default as usePermissions } from '@/shared/hooks/usePermissions';

// Generic table and UI hooks
export { useModal, useModalWithData,useMultiModal } from '@/hooks/useModal';