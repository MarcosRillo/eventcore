/**
 * Shared Components - Centralized Exports
 *
 * This is the canonical location for all shared UI components.
 * Import from @/shared/components for the most direct access.
 *
 * @example
 * import { Button, Modal, StatCard } from '@/shared/components'
 */

// Alert components
export * from './alerts'

// Display components (Badge, Card, SafeImage, Tooltip)
export * from './display'

// Feedback components (Loading, Skeleton, Empty, Toast)
export * from './feedback'

// Form components (Button, Input, Select, etc.)
export * from './form'

// Modal components (Modal, Dialogs, FormModal)
export * from './modals'

// Stats components (StatCard, StatsGrid)
export * from './stats'

// Table components (Table, Pagination, etc.)
export * from './tables'
